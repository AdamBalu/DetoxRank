# Copy this file to `keystore.properties` (same folder) and fill in your
# UPLOAD keystore details. keystore.properties is git-ignored — it never gets
# committed. Do NOT put the file this points to inside the repo.
#
# storeFile: absolute path to your upload keystore, e.g.
#   C:/Users/Ady/keys/detoxrank-upload.jks
storeFile=
storePassword=
keyAlias=
keyPassword=
